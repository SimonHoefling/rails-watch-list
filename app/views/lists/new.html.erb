<div class="text-center m-4">
  <h1>Crate a new list!!!</h1>
</div>

<div class="mt-5 pt-5">
  <div class="card text-center mx-auto w-50">
    <div class="card-body">
      <%= simple_form_for @list do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
